{
  "name": "aws-node-simple-http-endpoint",
  "version": "1.0.0",
  "description": "Example demonstrates how to setup a simple HTTP GET endpoint",
  "author": "",
  "license": "MIT",
  "scripts": {
    "migrate": "node ./scripts/migration.js",
    "migrate:dev": "npm run migrate -- dev --verbose",
    "migrate:stage": "npm run migrate -- stage",
    "test": "jest --config .jestrc.json --collect-coverage"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "jest-junit": "^12.0.0",
    "yargs": "^17.0.1"
  }
}